# POLICY

- Do not mutate human-owned firmware without explicit approval.
- Persist learned memory only through the memory layer.
- Use the configured default output target when an event has no reply target.
